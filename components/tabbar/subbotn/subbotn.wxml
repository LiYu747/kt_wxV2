<view>
  <view class="booy">
  	<view class="flex-d al-center itemBox"  wx:for="{{icondata}}"  wx:key='id' > 
  	      <image  bindtap="add" data-item="{{item}}" src="{{item.icon}}" class="oic1 {{index==1?'oic2':''}}" mode=""></image>
  			 <view bindtap="add"  data-item="{{item}}" class="itemtext  {{index==0?'calor':''}}">
  			 	{{item.titel}}
  			 </view> 
  	</view>
  </view>
</view>
